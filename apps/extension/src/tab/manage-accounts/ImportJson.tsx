import { Card, CardContent, CardHeader } from 'ui';

import Layout from '../layout/Layout.inner';

export const ImportJson = () => (
  <Layout>
    <Card>
      <CardHeader>Import JSON</CardHeader>
      <CardContent className='grid gap-4'>...</CardContent>
    </Card>
  </Layout>
);
