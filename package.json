{
  "name": "polkadot-lite-extension",
  "packageManager": "pnpm@8.7.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format:check": "prettier . --check",
    "format:fix": "prettier . --write",
    "extension:start": "pnpm --filter extension start"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "prettier": "^3.0.2",
    "turbo": "latest"
  }
}
